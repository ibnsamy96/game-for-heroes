import { TrueAnswerDirective } from './true-answer.directive';

describe('TrueAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new TrueAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
