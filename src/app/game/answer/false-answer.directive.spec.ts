import { FalseAnswerDirective } from './false-answer.directive';

describe('FalseAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new FalseAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
